{% extends 'base.html.twig' %}

{% block body %}
    <h1>Joueur</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ joueur.id }}</td>
            </tr>
            <tr>
                <th>Pseudo</th>
                <td>{{ joueur.pseudo }}</td>
            </tr>
            <tr>
                <th>Mdp</th>
                <td>{{ joueur.mdp }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ joueur.email }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('administration_joueur_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('administration_joueur_edit', { 'id': joueur.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
