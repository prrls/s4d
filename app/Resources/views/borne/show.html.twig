{% extends 'base.html.twig' %}

{% block body %}
    <h1>Borne</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ borne.id }}</td>
            </tr>
            <tr>
                <th>Ordre</th>
                <td>{{ borne.ordre }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('administration_borne_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('administration_borne_edit', { 'id': borne.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
