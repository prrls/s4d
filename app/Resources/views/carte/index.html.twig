{% extends 'base.html.twig' %}

{% block body %}
    <h1>Cartes list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for carte in cartes %}
            <tr>
                <td><a href="{{ path('administration_carte_show', { 'id': carte.id }) }}">{{ carte.id }}</a></td>
                <td>{{ carte.numero }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('administration_carte_show', { 'id': carte.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('administration_carte_edit', { 'id': carte.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('administration_carte_new') }}">Create a new carte</a>
        </li>
    </ul>
{% endblock %}
